# Jev Code Judge 企画書

## 1. 企画概要

**Jev Code Judge**は、正常なコードに意図的な変更（Mutation）を段階的に加え、Jevの判定がどう変化するかを観察する実験プロジェクトである。

> **Break the code. Find the boundary.**  
> コードを壊して、AIの判断境界を探す。

目的はWebアプリの開発そのものではない。Jevを使った実験から技術的な発見を得て、その方法・結果・考察をQiita記事として公開する。Webアプリは、実験と可視化のための装置として設計する。

中心となる問いは次のとおり。

> コードを少しずつ壊したとき、Jevはどの変更に反応し、どの時点で「本番投入NG」と判断するのか。

### 最終成果物

1. 実験用Webアプリ「Jev Code Judge」
2. Mutationごとの判定結果を記録したデータセット
3. 実験方法・結果・考察をまとめたQiita記事

---

## 2. 記事の狙い

一般的なAIコードレビューのように、問題点や修正案を出力させることが目的ではない。同じコードに変更を一つずつ加え、同じ質問を繰り返すことで、Jev自身の判断特性を観察する。

```text
正常なコード
  ↓
Mutationを一つ適用
  ↓
Jevで判定
  ↓
さらにMutationを適用
  ↓
判定の変化を記録・可視化
```

単なる利用紹介ではなく、判断境界（Decision Boundary）、感度分析（Sensitivity Analysis）、反実仮想的な説明（Counterfactual Explanation）へ話を広げ、実験記事として成立させる。

### 記事タイトル案

第一候補：

> **コードを少しずつ壊して、Jevが「本番NG」と判断する境界を可視化してみた**

その他の候補：

- Jevにコードを壊し続けて見せたら、どこで「本番NG」になるのか
- Jev Code Judge：コードMutationによる判断AIのDecision Boundary探索

第一候補は、実験内容がひと目で分かり、「コードを壊す」という親しみやすさと技術的なテーマを両立できる。

---

## 3. 検証したい問いと仮説

### 検証したい問い

1. コード品質を段階的に悪化させると、Jevの`REJECT`判定は強くなるか。
2. 型安全性、例外処理、入力検証、SQL Injection、`eval`、Secretのハードコード、深いネスト、命名のうち、何に強く反応するか。
3. どの変更を加えた時点で、判定が`SHIP`から`REJECT`へ反転するか。
4. 質問の観点を変えると、同じコードに対する判定も変わるか。
5. ESLintやSemgrepなどの静的解析と、Jevの判断にはどのような違いがあるか。
6. `REJECT`されたコードをどこまで直せば、再び`SHIP`と判断されるか。

### 仮説

- `eval()`、SQL文字列結合、Secretのハードコードなど、明確な危険性を持つ変更には強く反応する。
- `any`、深いネスト、命名の悪化など、文脈に依存する品質問題にも一定の反応を示す。
- 静的解析が明確なルール違反の検出を得意とする一方、Jevは「本番へ投入してよいか」という複数要素を含む判断に反応する。
- Mutationを積み重ねると、ある地点から`REJECT`スコアが大きく上昇する可能性がある。

これらは結論ではなく、実験前に置く仮説である。期待どおりの結果が出なかった場合も、その差異を観察結果として扱う。

---

## 4. 実験対象と判定形式

Mutationの意味と影響を追いやすくするため、20〜40行程度の小さなAPIコードを使用する。第一候補は、入力検証、型、データベースアクセス、例外処理を含むUser APIである。

```text
POST /users
```

基準コードの例：

```ts
async function findUser(id: string) {
  if (!id) {
    throw new Error("id is required");
  }

  return db.query(
    "SELECT * FROM users WHERE id = ?",
    [id]
  );
}
```

Jevには、次のような質問を与える。

> このコードを本番環境へデプロイして問題ないか？

判定形式は、次の3段階を想定する。

```text
SHIP
CAUTION
REJECT
```

各判定の数値を取得できる場合は、推移も記録する。ただし、その値を「確率」と呼べるかは仕様を確認する。校正された確率でない場合は、「判定スコア」または「信頼度」と表記する。

---

## 5. Mutationの設計

MVPでは、影響の違いが比較しやすい5種類に絞る。

| Mutation | 変更内容 | 主な観点 |
| --- | --- | --- |
| Remove Validation | 入力検証を削除 | 安全性・堅牢性 |
| Replace Type With `any` | 引数の型を`string`から`any`へ変更 | 型安全性 |
| Swallow Exception | `catch`内の記録と再送出を削除 | 障害対応・可観測性 |
| SQL String Concatenation | プレースホルダーを文字列埋め込みへ変更 | SQL Injection |
| Add `eval` | ユーザー入力を`eval()`へ渡す | 任意コード実行 |

変更例：

```ts
// Clean
db.query(
  "SELECT * FROM users WHERE id = ?",
  [id]
);

// Mutated
db.query(
  `SELECT * FROM users WHERE id = ${id}`
);
```

本企画における「Mutation」は、テストスイートの有効性を測る一般的なMutation Testingそのものではなく、評価対象のコードを意図的に変化させる実験操作を指す。記事では両者を混同しないよう説明する。

---

## 6. 実験計画

### 実験1：段階的な劣化と判断境界

同じコードへMutationを一つずつ加え、各段階の判定を記録する。`SHIP`と`REJECT`の優勢が入れ替わった地点、または`REJECT`スコアが大きく上昇した地点を判断境界の候補として観察する。

以下の数値は表示例であり、実測値ではない。

| Step | 適用したMutation | REJECTスコア（例） |
| ---: | --- | ---: |
| 0 | なし（Clean） | 0.04 |
| 1 | `any`へ変更 | 0.09 |
| 2 | 入力検証を削除 | 0.21 |
| 3 | 例外を握りつぶす | 0.38 |
| 4 | SQLを文字列結合へ変更 | 0.67 |
| 5 | `eval()`を追加 | 0.91 |

累積実験では順序が結果に影響する可能性があるため、代表的な順序を複数試す。「4個目だから反応した」のか、「特定の変更へ反応した」のかを区別する。

### 実験2：Mutationごとの感度分析

Clean CodeへMutationを一つだけ加え、それぞれを独立に評価する。Clean Codeとの差分を比較し、どの変更が判定へ強く影響したかを確認する。

### 実験3：質問の観点による違い

同じコードに対し、質問だけを変えて判定する。

- **Production Judge**：「このコードを本番へデプロイして問題ないか？」
- **Security Judge**：「このコードには重大なセキュリティリスクがあるか？」
- **Maintainability Judge**：「このコードは長期的に保守可能か？」

「危険なコードを検知できたか」だけでなく、質問の定義によってJevが注目する特徴がどう変わるかを考察する。

### 実験4：静的解析との比較

ESLint、Semgrep、Jevへ同じコードを入力し、何を検出・評価したかを比較する。優劣を決めるのではなく、各手法が対象とする問題の違いを明らかにする。

| 問題 | ESLint | Semgrep | Jev |
| --- | :---: | :---: | :---: |
| `eval()` | ○ | ○ | ○ |
| SQL Injection | △ | ○ | ○ |
| Hardcoded Secret | △ | ○ | ○ |
| 深いネスト | △ | △ | ○ |
| 不適切な命名 | △ | × | △ |
| 入力検証の欠落 | × | △ | ○ |
| 本番投入可否の総合判断 | × | × | ○ |

この表は現時点の仮説であり、実験後に実測結果へ置き換える。

### 実験5：どの修正で判定が戻るか

`REJECT`されたコードを起点に、修正を一つずつ適用する。判定を変えるために必要な修正を探り、どの修正が最も大きく影響したかを調べる。

```text
SQLをパラメータ化：0.89 → 0.43
入力検証を追加　　：0.89 → 0.67
anyを具体的な型へ ：0.89 → 0.81
```

上記は表示例であり、実測値ではない。この実験は、反実仮想的な説明として位置づける。

---

## 7. 実験条件と再現性

比較可能なデータを得るため、以下の条件を固定して記録する。

- 使用したJevのモデルとバージョン
- プロンプト全文と出力形式
- 推論パラメータ（設定できる場合）
- 基準コードと各Mutationの差分
- Mutationの適用順序
- 実行日時
- 各条件の試行回数

出力に揺らぎがある場合は各条件を複数回実行し、平均値だけでなく分布やばらつきも示す。単発のスコアだけで境界を断定せず、再試行しても傾向が維持されるかを確認する。

実験上の制約も明示する。

- 判定はプロンプトの表現に影響される可能性がある。
- Mutationを累積すると、個々の変更の効果が相互に影響する可能性がある。
- 一つのサンプルコードだけで、Jevの一般的な性能を結論づけることはできない。
- スコアが校正された確率でなければ、「危険である確率」とは解釈できない。

---

## 8. Web UI

UIは、実験を実行し、結果をQiita上で直感的に伝えるために作る。スクリーンショット、GIF、グラフ、Mutation履歴を記事へそのまま利用できる構成にする。

```text
┌─────────────────────────────────────────────────┐
│ JEV CODE JUDGE                                  │
│ Break the code. Find the boundary.              │
├───────────────────────┬─────────────────────────┤
│ CODE                  │ VERDICT                 │
│ async function ...    │ REJECT 82%              │
│                       │ Security        91      │
│                       │ Maintainability 54      │
│                       │ Production      31      │
├───────────────────────┴─────────────────────────┤
│ MUTATIONS                                       │
│ [Remove Validation] [Add any] [Swallow Catch]  │
│ [SQL Concatenation] [Add eval]                  │
├─────────────────────────────────────────────────┤
│ REJECT SCORE                                    │
│ 1.0 |                              ●            │
│ 0.5 |                  ●                        │
│ 0.0 | ●────────────────────────────             │
│       clean any catch sql eval                  │
└─────────────────────────────────────────────────┘
```

### MVPの必須機能

- Monaco Editorによるコード表示
- `SHIP`、`CAUTION`、`REJECT`の判定表示
- 5種類のMutationボタン
- 各判定の履歴保存
- `REJECT`スコアの推移グラフ
- Clean Codeへ戻すリセット機能
- 実験結果のJSONまたはCSV出力

### MVPの対象外

- ログイン、ユーザー管理
- データベースへの永続化
- SNS共有
- 高度なエディター機能
- 自動コード修正
- 本格的なCI/CD連携

機能追加の判断基準は、「Qiita記事の実験または説明に使うか」とする。

---

## 9. 技術構成

| 領域 | 採用技術 |
| --- | --- |
| Frontend | Next.js、React、TypeScript |
| UI | Tailwind CSS、shadcn/ui |
| Editor | Monaco Editor |
| Visualization | Recharts |
| Backend | Next.js Route Handler |
| Judge | Jev |

ディレクトリ構成案：

```text
app/
├── page.tsx
└── api/
    └── judge/
        └── route.ts

components/
├── CodeEditor.tsx
├── VerdictPanel.tsx
├── MutationPanel.tsx
├── JudgmentChart.tsx
└── MutationTimeline.tsx

lib/
├── jev.ts
├── mutations.ts
└── experiments.ts
```

---

## 10. 保存する実験データ

各試行について、次の項目を保存する。

```text
experimentId
trialNumber
modelVersion
prompt
mutation
mutationOrder
code
verdict
SHIP score
CAUTION score
REJECT score
timestamp
```

最終的にCSVまたはJSONとして出力する。記事執筆時にスクリーンショットしか残っていない事態を防ぎ、集計や再検証も可能にする。

---

## 11. 記事で使用するビジュアル

### スクリーンショット

1. Clean Codeの判定
2. Mutationの適用途中
3. `REJECT`へ変化した状態
4. Mutation Timeline
5. 判定スコアの推移グラフ
6. SecurityとMaintainabilityの比較

### GIF

Mutationを加えるたびに判定とグラフが変わる様子を、10〜20秒程度で見せる。

```text
Clean
  ↓
Add any
  ↓
Remove Validation
  ↓
SQL Concatenation
  ↓
Add eval
  ↓
REJECT
```

記事冒頭またはアプリ紹介の直後へ配置し、実験内容を短時間で伝える。

---

## 12. Qiita記事の構成

1. **AIコードレビューは一般的になった**  
   通常のレビューではなく、AIがどこで「本番NG」と判断するかを調べる目的を示す。
2. **コードを少しずつ壊したら分かるのでは？**  
   正常なコードとMutationの考え方を説明する。
3. **実験装置「Jev Code Judge」を作った**  
   Web UIと実験フローをスクリーンショットまたはGIFで紹介する。
4. **実験条件**  
   モデル、プロンプト、試行回数、スコアの扱いを明示する。
5. **コードを一つずつ壊す**  
   Mutationの累積による判定の推移を示す。
6. **Jevはどこで「本番NG」と判断したか**  
   判定が反転した地点と、その変更内容を分析する。
7. **どの変更に敏感だったか**  
   Mutationを独立に適用した感度分析を示す。
8. **質問を変えると判断も変わるか**  
   Security、Maintainability、Productionの違いを比較する。
9. **静的解析との違い**  
   ESLint、Semgrep、Jevが扱う問題の違いを整理する。
10. **どこを直せば`SHIP`へ戻るか**  
    修正による判定変化を反実仮想的に分析する。
11. **分かったことと限界**  
    実験結果、再現性、誤判定、一般化の限界をまとめる。
12. **実システムへ組み込む場合の考察**  
    CI/CDでの閾値設定や静的解析との併用を検討する。

### 記事冒頭案

> AIコードレビュー自体は、もう珍しくありません。  
> そこで今回は少し方向を変えて、「コードを少しずつ壊していったら、AIはどこで“本番投入NG”と言い始めるのか？」を調べてみることにしました。  
> 判断にはJevを使います。正常なコードから始めて、`any`、入力検証の削除、例外の握りつぶし、SQL文字列結合、`eval()`と、徐々にコードを壊します。そのたびに同じ質問を投げ、判定の変化を可視化します。  
> 今回観察するのは、AIがコードのどこで「これは本番に出せない」と判断するかです。

---

## 13. 考察するポイント

- **スコアの解釈**：`REJECT 0.82`を何の値として扱えるか。校正された確率でなければ、「82%の確率で危険」とは解釈しない。
- **閾値の設定**：CIのゲートとして利用する場合、False PositiveとFalse Negativeを踏まえて基準を検討する。
- **静的解析との併用**：ルールベースの検査を置き換えるのではなく、総合判断を加える用途を検討する。
- **判断の安定性**：同じ入力での揺らぎ、Mutationの順序、プロンプト表現の影響を確認する。

期待どおりの結果が出ること自体を成功条件にはしない。`any`へ変更しても反応しない、深いネストを問題視しない、コメントだけで判定が変わる、といった結果もJevの判断特性を知るうえで価値がある。

---

## 14. 追加実験候補

MVPの実験が完了し、記事の分量に余裕がある場合に実施する。

- **Mutationの順序を変える**：`any → SQL → eval`と`eval → any → SQL`で結果を比較する。
- **コメントだけを変える**：`// Temporary workaround`や`// DO NOT TOUCH`が判断に影響するかを調べる。
- **変数名だけを変える**：`userInput`を`trustedInput`へ変えた場合の判定差を見る。
- **複数のサンプルコードで再試験する**：特定のコードに依存した結果かを確認する。

---

## 15. 企画の成功条件

本企画は、次の状態をもって完了とする。

- 定義したMutationをUIから適用し、各段階でJevの判定を取得できる。
- 実験条件と判定結果をCSVまたはJSONで保存できる。
- 累積実験、独立実験、質問別比較、修正実験の結果を可視化できる。
- 結果の再現性と限界を確認できる。
- 実測データとビジュアルを用いて、Qiita記事を公開できる状態になっている。

企画を一文で表すと、次のとおりである。

> **コードを段階的に変化させながらJevの判定スコアを追跡し、判断AIがどのコード特徴に反応し、どこで「本番NG」へ判断を反転させるのかを可視化する。**

